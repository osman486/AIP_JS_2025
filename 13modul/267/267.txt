1)! — функция вызывается сразу, результат ('!') присваивается в result

2)ƒunction() { return '!'; } — в result записывается сама функция, а не ее результат

3)! — сначала функция присваивается в result, затем вызывается через result()

