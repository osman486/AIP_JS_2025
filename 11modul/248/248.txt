1)1 — внутренняя функция видит переменную внешней функции

2)Ничего — функция func объявлена, но не вызвана

3)1 — функция func вызывается и видит переменную num

4)1 и 2 — сначала func вызывается после присвоения num = 1, потом после num = 2

