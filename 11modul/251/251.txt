1)1 — передается внешний num как параметр внутренней функции

2)1 — изменение локального параметра num внутри func не влияет на внешнюю переменную

3)2 — перед вызовом func внешний num изменен на 2, передается 2

4)1 — func вызывается до изменения внешнего num

