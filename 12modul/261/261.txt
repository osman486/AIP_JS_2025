1)3 — внутренняя функция видит num1 (1) и num2 (2) из лексического окружения

2)3 — вызов test()() сразу, результат тот же

3)3 — num1 из лексического окружения (1), num2 из глобальной области (2)

4)1 — внутренняя функция использует num из лексического окружения (1), а не глобальную переменную num (2)

