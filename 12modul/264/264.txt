1)0 0 0 — каждый вызов func() создает новую функцию с новым замыканием, где num каждый раз начинается с 0

2)0 0 0 — то же самое, test просто ссылка на func, каждый вызов создает новую независимую функцию с num = 0